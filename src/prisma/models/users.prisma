model Users {
  id        String    @id @default(cuid())
  email     String    @unique
  firstName String
  lastName  String
  password  String
  role      UserRole
  fiddles   Fiddles[]
}

enum UserRole {
  FREELANCER
  EMPLOYER
}
