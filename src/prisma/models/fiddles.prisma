model Fiddles {
  id        String         @id @default(cuid())
  name      String
  content   String         @db.Text
  user      Users          @relation(fields: [userId], references: [id])
  userId    String
  images    FiddleImages[]
  proposals Proposals[]
}
